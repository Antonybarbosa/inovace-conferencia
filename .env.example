# Variáveis usadas pelo docker-compose.yml (raiz do projeto).
# Copie para .env caso precise mudar a porta publicada do frontend.
#
# Porta do host que serve o frontend. Padrão: 80.
# Use outra (ex.: 8080) se a 80 já estiver ocupada no servidor.
FRONTEND_PORT=80
